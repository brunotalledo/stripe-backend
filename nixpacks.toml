[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = [
  "npm install --include=dev",
  "npm rebuild stripe || true"
]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "node server.js"
