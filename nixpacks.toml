[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = [
  "npm install --production=false",
  "npm rebuild"
]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "node server.js"
